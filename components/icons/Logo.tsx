const Logo = ({ ...props }) => (
  <span
    {...props}
    style={{ fontWeight: 700, fontSize: '1.25rem', letterSpacing: '-0.02em', color: 'white' }}
  >
    unseen
  </span>
);

export default Logo;
